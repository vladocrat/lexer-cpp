#include <iostream>

#include "lexer.h"

int main(char* argc, char** argv) {
    double a = 0.0;

    if (sqrt * 2 == 3) {
        a = a + 2;
    } else {
        echo("outer else");
    }

    return 0;
}